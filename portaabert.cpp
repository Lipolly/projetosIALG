#include <iostream>
using namespace std;
int main() {
	
	int num, num2, a, n;
	
    cin >> num;
    cin >> num2;
    
    n = 0;
    a = 0;
    do {
		n = n + num2;
		a = a + 1;
		if (n < num) {
		};
		}while(n < num);
		cout << a << endl;
		return 0;
		}
